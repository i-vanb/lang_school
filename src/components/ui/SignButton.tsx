// import {Locale} from "@/i18n.config";
// import { getDictionary } from '@/lib/dictionary'
//
//
// export const SignInButton = async ({lang}: { lang: Locale }) => {
//   const { header } = await getDictionary(lang)
//
//   return (
//     <button onClick={()=>signIn()}>{header.sign_in}</button>
//   );
// }
